[versions]

fancymodloader = "12.0.0"
forgegradle = "7.0.40"

[libraries]

fancymodloader = { group = "net.neoforged.fancymodloader", name = "loader", version.ref = "fancymodloader" }

[plugins]

forgegradle = { id = "net.minecraftforge.gradle", version.ref = "forgegradle" }
